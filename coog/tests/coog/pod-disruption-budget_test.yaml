suite: Test coog pod disruption budget
templates:
  - coog/pod-disruption-budget.yaml
release:
  name: test
  namespace: test
tests:
  - it: should create PDB when backCore is enabled
    set:
      backCore:
        enabled: true
        coog:
          podDisruptionBudget:
            minAvailable: 2
    asserts:
      - isKind:
          of: PodDisruptionBudget
      - equal:
          path: spec.minAvailable
          value: 2
      - matchRegex:
          path: metadata.name
          pattern: -coog$
      - equal:
          path: spec.selector.matchLabels["app.kubernetes.io/name"]
          value: coog
      - equal:
          path: spec.selector.matchLabels["app.kubernetes.io/instance"]
          value: test

  - it: should use specific PDB config when component-specific is not set
    set:
      podDisruptionBudget:
        minAvailable: 2
      backCore:
        enabled: true
        coog:
          podDisruptionBudget:
            minAvailable: 3
    asserts:
      - equal:
          path: spec.minAvailable
          value: 3
  
  - it: should use global PDB config when component-specific is not set
    set:
      podDisruptionBudget:
        minAvailable: 3
      backCore:
        enabled: true
    asserts:
      - equal:
          path: spec.minAvailable
          value: 3 