apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coog.fullname" . }}-test-connection"
  labels:
{{ include "coog.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "coog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
