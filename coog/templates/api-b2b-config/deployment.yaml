{{- if .Values.apiB2bConfig.enable -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
spec:
{{- if not .Values.quota.rolling_update }}
  strategy:
    type: Recreate
{{- end }}
  replicas: {{ default .Values.replicaCount .Values.apiB2bConfig.replicaCount }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
    spec:
    {{- with .Values.apiB2bConfig.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: {{ include "common.names.name" . }}
          image: "{{ .Values.apiB2bConfig.image.repository }}:{{ default .Values.image.tag .Values.apiB2bConfig.image.tag }}"
          imagepullPolicy: {{ default .Values.image.pullPolicy .Values.apiB2bConfig.image.pullPolicy | quote }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            procMount: Default
            runAsNonRoot: true
            runAsUser: {{ default .Values.securityContext.runAsUser .Values.apiB2bConfig.securityContext.runAsUser }}
          env:
            - name: COOG_URL
              value: "http://{{ .Release.Name }}-{{ .Chart.Name }}-coog"
            - name: COOG_DB_NAME
              value: "{{ .Values.postgresql.auth.database }}"
            - name:  JWT_INTERNAL_ENCRYPTION
              value: "{{ .Values.jwt.internal.encryption }}"
            {{- if .Values.apiB2bConfig.extraEnv }}
            {{- range $key, $value := .Values.apiB2bConfig.extraEnv }}
            - name:  {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.apiB2bConfig.resources | nindent 12 }}
      {{- with .Values.apiB2bConfig.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.apiB2bConfig.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.apiB2bConfig.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
{{- if .Values.apiB2bConfig.dnsPolicy }}
      dnsPolicy: {{ .Values.apiB2bConfig.dnsPolicy }}
{{- end }}
{{- if .Values.apiB2bConfig.dnsConfig }}
      dnsConfig:
{{ toYaml .Values.apiB2bConfig.dnsConfig | indent 8 }}
{{- end }}
{{- end }}
