{{- if .Values.secrets.backCore.cron.config -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-configuration" (include "common.names.fullname" .) | trunc 63 | trimSuffix "-" }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets.backCore.cron.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
