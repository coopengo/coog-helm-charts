{{- if .Values.customer_frontend.enable -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
spec:
  replicas: {{ default .Values.replicaCount .Values.customer_frontend.replicaCount }}
{{- if not .Values.quota.rolling_update }}
  strategy:
    type: Recreate
{{- end }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
    spec:
    {{- with .Values.customer_frontend.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
        - name: builddir
          emptyDir: {}
      initContainers:
        - name: "{{ .Chart.Name }}-build"
          image: "{{ .Values.customer_frontend.image.repository }}:{{ default .Values.image.tag .Values.customer_frontend.image.tag }}"
          imagepullPolicy: {{ default .Values.image.pullPolicy .Values.customer_frontend.image.pullPolicy | quote }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            procMount: Default
            runAsNonRoot: true
            runAsUser: {{ .Values.coog.securityContext.runAsUser }}
          resources:
            {{- toYaml .Values.customer_frontend.initContainers.resources | nindent 12 }}
          command: ['sh', '-c', 'yarn', 'build', '--prefix-paths']
          volumeMounts:
            - name: builddir
              mountPath: "/workdir/public"
          {{- if .Values.customer_frontend.env }}
          env:
            {{- range $key, $value := .Values.customer_frontend.env }}
            - name:  {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
      containers:
        - name: {{ include "common.names.name" . }}
          image: "{{ .Values.customer_frontend.image.repository }}:{{ default .Values.image.tag .Values.customer_frontend.image.tag }}"
          imagepullPolicy: {{ default .Values.image.pullPolicy .Values.customer_frontend.image.pullPolicy | quote }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            procMount: Default
            runAsNonRoot: true
            runAsUser: {{ .Values.coog.securityContext.runAsUser }}
          volumeMounts:
            - name: builddir
              mountPath: "/workdir/public"
          {{- if .Values.customer_frontend.extraEnv }}
          env:
            {{- range $key, $value := .Values.customer_frontend.extraEnv }}
            - name:  {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.customer_frontend.resources | nindent 12 }}
      {{- with .Values.customer_frontend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.customer_frontend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.customer_frontend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
{{- if .Values.customer_frontend.dnsPolicy }}
      dnsPolicy: {{ .Values.customer_frontend.dnsPolicy }}
{{- end }}
{{- if .Values.customer_frontend.dnsConfig }}
      dnsConfig:
{{ toYaml .Values.customer_frontend.dnsConfig | indent 8 }}
{{- end }}
{{- end }}
